meta {
  name: post -my-contracts--contractId--fulfill
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/my/contracts/{contractId}/fulfill
  body: none
  auth: inherit
}

auth:bearer {
  token: {{token}}
}
